<({common})>
<({formats})>
<({handlers})>

content         =  { "types" ~ header ~ recipe_content }
recipe_content  =  { templates ~ n ~ methods }
templates       =  { "templates" ~ i ~ keyed_formats ~ DROP }
methods         =  { "methods" ~ i ~ function ~ (n ~ function)* ~ DROP }
function        =  { key ~ i ~ (variables ~ n)? ~ sameline_handlers ~ DROP }
