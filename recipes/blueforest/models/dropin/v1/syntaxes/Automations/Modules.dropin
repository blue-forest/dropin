syntaxes Modules
================

module $patterns.syntax "\n"{1..} $patterns.function{1..}

syntax $patterns.id

id $patterns.key ":" $patterns.key ":" $patterns.key ":" $patterns.key

function "fn " $patterns.key "()\n" $patterns.command{..}

command $patterns.localCommand | $patterns.metaCommand

localCommand ">" $patterns.metaCommand

metaCommand $patterns.print

print "print \"" $patterns.message "\""

message $patterns.message_char+

message_char "\\\"" | !"\""
